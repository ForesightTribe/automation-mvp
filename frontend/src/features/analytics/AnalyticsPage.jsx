import { PagePlaceholder } from "../../components/feedback/PagePlaceholder";

/** Sales & Analytics — "where is revenue coming from". See dashboard-views.md. */
export const AnalyticsPage = () => (
	<PagePlaceholder
		title="Sales & Analytics"
		subtitle="Where is revenue coming from."
	/>
);
