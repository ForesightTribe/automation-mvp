import { PagePlaceholder } from "../../components/feedback/PagePlaceholder";

/** Ops / Settings — watchlist, platform connections, scrape jobs. See dashboard-views.md. */
export const SettingsPage = () => (
	<PagePlaceholder
		title="Settings"
		subtitle="Watchlist, connections, and scrape jobs."
	/>
);
