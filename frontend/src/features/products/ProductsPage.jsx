import { PagePlaceholder } from "../../components/feedback/PagePlaceholder";

/** Products — "per-SKU deep dive". See dashboard-views.md. */
export const ProductsPage = () => (
	<PagePlaceholder title="Products" subtitle="Per-SKU deep dive." />
);
