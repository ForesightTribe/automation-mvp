BASE_URL = "https://partnersbiz.com"

# ── Login ──────────────────────────────────────────────────────────────────────
LOGIN_EMAIL_INPUT = 'input[placeholder="Enter Email ID"]'
LOGIN_REQUEST_OTP_BUTTON = 'button:has-text("Request OTP")'
LOGIN_OTP_INPUTS = 'input[inputmode="numeric"][maxlength="1"]'
LOGIN_SUBMIT_OTP_BUTTON = 'button:has-text("Submit OTP")'

# Account selection screen (appears after OTP when multiple companies are linked)
LOGIN_ACCOUNT_CARD = 'span.ant-typography-ellipsis-single-line'
